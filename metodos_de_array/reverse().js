/* el metodo reverse() invierte el orden de los elementos de un array en su lugar. El primer elemento pasa a ser el ultimo y el ultimo pasa a ser le primero. La llamada a reverse() devuelve una referencia al array invertido. */

const array1 = [2, 6, 9, 0];

const reversed = array1.reverse();

console.log(reversed);
