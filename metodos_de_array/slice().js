/* el metodo slice devuelve una copia de una parte del array dentro de un nuevo array empezando del inicio hasta el fin (fin no incluido). El array original no se modificara. */

const array1 = [13, 5, 6, 88, 9];

const array2 = array1.slice(1);

console.log(array2);
