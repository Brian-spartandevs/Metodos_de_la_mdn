/* el metodo shift() elimina el primer elemento del array y lo retorna. Este metodo modifica la longitud del array. */

const array1 = [1, 2, 3, 4];

console.log(array1.shift());

console.log(array1);
