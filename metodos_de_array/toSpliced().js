/* el metodo toSpliced() de las instancias de Array. es la copia del metodo splice(). Devuelve una nueva matriz con algunos elementos eliminados y/o reemplazados de un indice determinado. */
//nota: este es un nuevo metodo que saldra, a la fecha del subido este codigo no aparece nada.

const array = [1, 2, 3, 4, 5];

const newArray = array.toSpliced(1, 2, 10);

console.log(newArray);
