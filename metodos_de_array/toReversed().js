/* el metodo toReversed() de las instancias de array es la contraparte de copia del metodo reverse().  Devuelve una matriz con los elementos en orden invertido. */
//nota: este es un nuevo metodo que saldra, a la fecha del subido este codigo no aparece nada.

const array = [1, 2, 3, 4, 5];

const newArrayReversed = array.toReversed();

console.log(newArrayReversed);
