/* el metodo map() crea un nuevo array con los resutados de la llamada a la funcion indicada aplicados a cada uno de sus elementos. */

const array1 = [12, 7, 24, 76, 21, 0];

const result = array1.map((element) => element * 2);
console.log(result);
