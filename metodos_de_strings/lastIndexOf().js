/* el metodo lastIndexOf() devuelve la posicion (indice) en la que se encuentra el valorBusqueda, dentro del objeto string que realiza la llamada, de la ultima ocurrencia del valor especificado; o -1 si no se halla. La busqueda se realiza empezando por el final de la cadena que realiza la llamada. */

const str1 = "Saturday";

console.log(str1.lastIndexOf("a"));
