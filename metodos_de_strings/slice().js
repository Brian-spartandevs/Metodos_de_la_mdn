/* El método slice() extrae una sección de una cadena y Lo devuelve como una nueva cadena, sin modificar la cadena original. */

const str = 'The quick brown fox jumps over the lazy dog.';

console.log(str.slice(31));